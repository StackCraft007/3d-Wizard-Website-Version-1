# Render service configuration for backend2
services:
  - type: web
    name: backend2-stl-volume
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: production
